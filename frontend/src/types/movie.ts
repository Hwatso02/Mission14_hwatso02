export type Movies = {
  //same column names an data types as database
  movieId: number;
  category: string;
  title: string;
  year: number;
  director: string;
  edited: string;
  lentTo: string;
  notes: string;
};
